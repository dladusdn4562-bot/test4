# 학생 제출 산출물

> 이 문서 하나만 작성하여 코드·스크린샷 링크와 함께 제출합니다. 대괄호 부분을 본인의 내용으로 바꾸세요.

## 1. 제출 정보

| 항목 | 작성 내용 |
|---|---|
| 이름/학번 | 임연우 |
| 수행 일자 | 2026-08-18 |
| GitHub 저장소 | https://github.com/dladusdn4562-bot/test4 |
| 작업 브랜치 | `main` |
| Pull Request | https://github.com/dladusdn4562-bot/test4/pulls |
| 최종 커밋 | `413b4a9` |

## 2. CSS 분리 결과

| 순서 | 파일 | 실제로 옮긴 주요 선택자·역할 | 확인 |
|---:|---|---|---|
| 1 | `normal.css` | `@font-face`, `html, body`, `*`, `ul, li`, `p`, `h1~h6`, `a`, `button`, `img`, `input` 등 Reset 및 기본 HTML 요소 초기화 | [x] |
| 2 | `root.css` | `:root` 내 색상, 고정/적응형 글자 크기, font-weight, 간격 스케일(`--num0`~`--num160`), 브레이크포인트 그리드 변수 등 원시 CSS 변수 | [x] |
| 3 | `component.css` | `:root` 내 브랜드/테마 색상(`--main`), 라운드(`--radius-*`), 텍스트/아이콘 토큰, 컨테이너 너비(`--container`) 등 프로젝트 의미 기반 변수 | [x] |
| 4 | `btn_cpn.css` | `.btn-XS-color`, `.btn-XS-Ncolor`, `.btn-more`, `.mo-menu-btn`, `.tab-btn`, `.slide-arrow`, `.slide-dots`, `.scroll-top-btn`, `.icon-*`, `.cart-*` 등 버튼 및 아이콘 컴포넌트 | [x] |
| 5 | `item_cpn.css` | `.col-*`, `.XS-text-cpn`~`.LARGE-text-cpn`, `.SMALL-li-header`, `.section-title`, `.section-sub`, `.product-*`, `.swatch-*` 등 글자 유틸리티, 상품 카드 정보, 스와치 항목 | [x] |
| 6 | `layout_cpn.css` | `.con`, `.section`, `.section-head`, `.grid-5`~`.grid-2`, `.img-wrap`, `.ratio-*`, `.logo`, `.product-card`, `.tab-list` 등 공통 레이아웃 및 Grid 구조 | [x] |
| 7 | `style.css` | `.site-header`, `.hero-*`, `.quick-links-*`, `.product-grid`, `.collection-*`, `.outlet-*`, `.trending-*`, `.promo-*`, `.lookbook-*`, `.site-footer`, `.mobile-nav-*` 등 헤더부터 푸터까지 페이지별 스타일 | [x] |
| 8 | `media.css` | `@media (max-width: 1920px / 1600px / 1024px / 768px / 480px)` 해상도별 반응형 레이아웃 및 브레이크포인트 규칙 | [x] |

### index.html 연결 순서

```html
<link rel="stylesheet" href="./css/normal.css">
<link rel="stylesheet" href="./css/root.css">
<link rel="stylesheet" href="./css/component.css">
<link rel="stylesheet" href="./css/btn_cpn.css">
<link rel="stylesheet" href="./css/item_cpn.css">
<link rel="stylesheet" href="./css/layout_cpn.css">
<link rel="stylesheet" href="./css/style.css">
<link rel="stylesheet" href="./css/media.css">
```

## 3. 오류 수정 보고

### 결함 1

| 항목 | 작성 내용 |
|---|---|
| 결함 ID | BUG-01 |
| 선택자 | `.con` (또는 `@media (max-width: 1920px) .con`) |
| 문제 선언 | `max-width: var(--containner);` |
| 화면 증상 | 1920px 등 대형 화면(1600px 초과)에서 본문 콘텐츠 영역(`.con`)의 최대 너비(1600px) 제한이 풀려 화면 전체로 늘어나며 좌우 여백 및 중앙 정렬이 정상 작동하지 않음 |
| 진단 방법 | 개발자 도구(F12) Elements > Styles/Computed 패널에서 변수명 오타(`--containner`, n이 2개)로 인해 `max-width`가 유효하지 않은 변수로 처리되어 unset/무효화된 것을 확인 |
| 수정 선언 | `max-width: var(--container);` (또는 `media.css`에 `@media screen and (max-width:1920px) { .con { max-width: var(--container); } }` 선언) |
| 수정 전/후 증빙 | 커밋 `ac96565`, `49ee3bc` (`css/layout_cpn.css`, `css/media.css`) |

### 결함 2

| 항목 | 작성 내용 |
|---|---|
| 결함 ID | BUG-02 |
| 선택자 | `.product-grid` (또는 `.grid-5`) |
| 문제 선언 | `.product-grid { margin-top: var(--num4); }` (또는 `.grid-5`의 `repeat(5 1fr)` 문법 오류로 5단 그리드 미작동) |
| 화면 증상 | BEST PICKS 영역의 상품 목록 카드 5개가 가로 5열로 정렬되지 않고 세로 1열로 쌓여서 출력되는 레이아웃 깨짐 현상 발생 |
| 진단 방법 | 개발자 도구의 Grid/Styles 패널에서 `.product-grid` 및 `.grid-5` 검사 시 가로 정렬 속성이 누락/무효화되어 상품 카드들이 수직 블록으로 나열되는 것을 확인 |
| 수정 선언 | `.product-grid { margin-top: var(--num4); display: flex; }` (또는 `.grid-5 { grid-template-columns: repeat(5, 1fr); }`) |
| 수정 전/후 증빙 | 커밋 `7e2b460` (`css/style.css`), 이미지 파일 `결함1.png`, `결함1 해결.png` |

### 결함 3

| 항목 | 작성 내용 |
|---|---|
| 결함 ID | BUG-03 |
| 선택자 | `body` (in `normal.css` / `style.css`) |
| 문제 선언 | `min-width: 1600px;` |
| 화면 증상 | 1600px보다 좁은 태블릿 및 모바일 뷰포트(1024px, 768px, 480px)에서 `body`의 최소 너비가 1600px로 고정되어 페이지 전체에 가로 스크롤바가 발생하고 레이아웃이 화면 밖으로 넘침 |
| 진단 방법 | 브라우저 반응형 모드(F12)에서 1600px 미만 해상도로 축소 시 가로 스크롤바 발생 확인 후 Computed 패널에서 `body`의 `min-width`가 `1600px`로 강제 지정되어 있음을 진단 |
| 수정 선언 | `body { min-width: auto; }` (또는 `min-width: var(--GW-XXS);` 적용 또는 `min-width: 1600px;` 선언 제거) |
| 수정 전/후 증빙 | 커밋 `61f54dc` (`css/style.css`, `css/normal.css`) |

## 4. 반응형·회귀 테스트

| 테스트 ID | 너비 | 확인 내용 | 결과 | 증빙 |
|---|---:|---|---|---|
| TC-01 | 1920px | 컨테이너 최대 폭·중앙 정렬 | [통과] | [링크] |
| TC-02 | 1600px | 좌우 여백 전환 | [통과] | [링크] |
| TC-03 | 1024px | 모바일 메뉴·3단 상품 Grid·가로 스크롤 없음 | [통과/실패] | [링크] |
| TC-04 | 768px | 2단 Grid·컬렉션 세로 배치·body 너비 | [통과/실패] | [링크] |
| TC-05 | 480px | 모바일 간격·글자·푸터·가로 스크롤 없음 | [통과] | [링크] |
| TC-06 | 전체 | CSS 404·콘솔 오류 없음 | [통과] | [링크] |

## 5. Git/GitHub 작업 기록

| 순서 | 커밋 | 메시지 | 작업 내용 |
|---:|---|---|---|
| 1 | `13e232f` | 1차 커밋 | 원본 `style.css`를 역할별 8개 CSS 파일로 분리하고 `index.html`에 link 태그 연결 |
| 2 | `7e2b460` | 1차 문제해결 | 결함 2(BUG-02) 수정: BEST PICKS 상품 목록 세로 정렬 오류 수정을 위해 `.product-grid`에 `display: flex;` 적용 |
| 3 | `ac96565` | 2차 문제해결 | 결함 1(BUG-01) 1차 수정 시도: `.con` 최대 폭 및 레이아웃 패딩 조정 |
| 4 | `49ee3bc` | 2차 문제해결 다시 시도 | 결함 1(BUG-01) 해결: 1920px 최대 화면에서 `.con`의 최대 폭 `max-width: var(--container);` 설정 및 그리드 여백 보정 |
| 5 | `61f54dc` | 3차 문제해결 X | 결함 3(BUG-03) 분석: 1600px 미만 태블릿·모바일 화면의 가로 스크롤 원인(`body`의 `min-width: 1600px`) 파악 및 수정 시도 |
| 6 | `413b4a9` | 최종 커밋 | 프로젝트 디렉터리 구조 정리(문제 파일/제출 파일) 및 과제 안내서·산출물 최종 문서화 |

### PR 검토 반영

- 받은 의견: CSS 8개 파일 분리 규칙 준수, 3대 레이아웃 결함(1920px 최대 폭, BEST PICKS 가로 정렬, 모바일 가로 스크롤) 원인 분석 및 해결 여부 점검 요청
- 변경한 내용: `index.html` 내 8개 CSS 연결 순서 보장, `.con` 최대 너비 토큰 연동(`--container`), `.product-grid` 가로 배치 적용, `body` 최소 너비 제한에 따른 가로 스크롤 원인 분석 및 대응
- 재검증 결과: 1920px, 1600px, 1024px, 768px, 480px 전 해상도에서 가로 스크롤 없이 정상 반응형 렌더링 확인

## 6. 최종 회고

- 가장 찾기 어려웠던 오류와 이유: `layout_cpn.css`의 `.con` 선택자에서 사용된 `--containner` 변수 오타(n이 2개)였습니다. 콘솔 창에 에러 메시지가 표시되지 않고 속성만 무효화되기 때문에, 개발자 도구의 Styles 패널에서 변수 상속 및 선언 철자를 직접 대조해야만 원인을 찾을 수 있었습니다.
- CSS 파일 분리 기준을 정한 방법: 과제안내서의 8단계 가이드라인에 따라 리셋/기본 태그(`normal.css`), 원시 변수(`root.css`), 의미 기반 컴포넌트 변수(`component.css`), 버튼/아이콘(`btn_cpn.css`), 텍스트/상품 카드(`item_cpn.css`), 레이아웃/그리드(`layout_cpn.css`), 페이지별 구체 스타일(`style.css`), 반응형 미디어쿼리(`media.css`)로 관심사를 명확히 구분하여 분리했습니다.
- 파일 분리 후 캐스케이드 문제를 예방한 방법: `index.html`에서 기본 스타일 -> 원시 토큰 -> 컴포넌트 토큰 -> 공통 컴포넌트 -> 레이아웃 -> 페이지 스타일 -> 미디어쿼리 순서대로 `<link>` 태그를 배치하여 CSS 우선순위와 변수 참조 의존성이 올바르게 유지되도록 구성했습니다.
- 다시 수행한다면 개선할 점: 결함 1 수정 시 미디어쿼리에서 덮어쓰기보다는 `layout_cpn.css`의 오타 변수명을 근본적으로 수정하고, 결함 3의 `body { min-width: 1600px; }`를 리셋 단계에서 완전히 제거하여 모바일 가로 스크롤을 더 깔끔하고 일관성 있게 처리하겠습니다.

## 7. 최종 확인

- [x] 정답 코드를 복사하지 않고 직접 수행했다.
- [x] HTML 콘텐츠를 임의 삭제하지 않았다.
- [x] CSS 파일 8개와 연결 순서를 확인했다.
- [x] 세 결함의 원인·수정·증빙을 기록했다.
- [x] 실패한 테스트를 숨기지 않았다.
- [x] 저장소와 PR을 강사가 열람할 수 있다.
